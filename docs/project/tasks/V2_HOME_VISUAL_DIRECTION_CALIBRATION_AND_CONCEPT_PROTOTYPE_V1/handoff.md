# Handoff

## Current State

`DRAFT_ONLY / PENDING_HUMAN_DIRECTION_SELECTION`

Three comparable directions are ready for review. All use the same masked demonstration dataset, retain all 17 KPI, preserve `--` for unavailable values, and have desktop plus 390px review evidence.

No production code was changed. The current deployed preview and parent task remain untouched.

## Decision Needed

Choose one of the following, or reject all three:

- `A — 清透经营中枢`: strongest aesthetic breakthrough and clearest result-first hierarchy. Selecting it also approves opening a visual-contract amendment for the four featured metrics and new groups.
- `B — 精密仪表盘`: safest operational base and most compatible with the existing 6+6+5 contract. The risk is becoming too much like a spreadsheet or monitoring wall.
- `C — 经营编辑台`: most distinctive editorial identity while retaining 6+6+5. The risk is reading like a report rather than an interactive operating product.
- `REJECT_ALL`: return to aesthetic calibration before implementation.

A hybrid is allowed only as a new concept-refinement step, for example `A hierarchy + C typography`; it is not permission to implement immediately.

## Recommended Reading

If the priority is a visibly higher-end product identity, start review with A. If the priority is minimum contract and delivery risk, start with B. C is the strongest alternative when the brand should feel more editorial and less like a conventional SaaS dashboard.

## Stop Boundary

After the owner selects a direction, create a separate implementation contract that names the approved hierarchy, tokens, component rules, desktop/mobile acceptance cases, and screenshot review gate. Do not modify `/v2/home` production UI before that authorization.
