# CONTEXT_PACK

## Latest direct owner instruction

On 2026-07-20 the owner directly authorized continuation with these active rules:

- The current `/v2/home` visual direction is accepted as implementation direction evidence: “这一版可以，可以继续沿着这个来推进”.
- This is not final acceptance for unimplemented pages or later route slices.
- Restore historical V2 detail from current SSOT, blueprint, and task contracts, including 17 KPI visibility/sorting, series/store/product centers, exclusion rules, search assets, target center, upload, data health, and day/week/month/custom/comparison behavior.
- Do not invent formulas and do not implement the deferred AI advisor.
- Historical runtime data may be deleted only when it is confirmed obsolete, documented, and safe.
- This task includes A2 implementation plus A3 deployment authorization for the authorized SaaS UI V2 continuation slices.

## Primary owner

- `ecommerce-platform-optimized`

## Cross-project intake dependency

- `/Users/zongji/Documents/个人助手搭建/AI_ASSISTANT_OS/jobs/2026-07-20_platform_visual_approved_continuation_and_p1c_material_v9/CONTEXT_PACK.md`

## Authority order

1. Latest direct owner instruction on 2026-07-20
2. `docs/project/PROJECT_SSOT.json`
3. `docs/project/current-task.json`
4. `docs/project/tasks/SAAS_V2_CONTINUATION_FROM_VISUAL_APPROVAL_AND_RUNTIME_CLEANUP_V1/task-contract.json`
5. `docs/product-blueprint-v2/AIRBURG_SAAS_PRODUCT_UI_BLUEPRINT_V2.md`
6. Route, metric, target, and validator contracts under `docs/project/**`
7. Historical SaaS UI V2 task folders under `docs/project/tasks/**`

## Current project truth at task start

- Active product track remains `SAAS_UI_V2`.
- `/v2/home` is already deployed and publicly verified with real upload18 data, but `visualAccepted=false` and `humanAccepted=false`.
- The previously current task `V2_HOME_UPLOAD18_AD_PLAN_LAYERING_AND_DEPLOY_GATE_V1` is complete through technical deployment and public regression, and remains open only for owner review.
- The other V2 routes still require implementation continuation and must not inherit `/v2/home` acceptance.

## Boundary decisions for this task

- Continue by blueprint priority after `/v2/home`: `series-board` -> `store-board` -> `product-board` -> `upload` + `data-health` -> `target-center` -> `search-assets` + `exclusion-rules`.
- Record current `/v2/home` approval as `DESIGN_DIRECTION_ACCEPTED_FOR_IMPLEMENTATION`, not as final route acceptance.
- Reuse existing V1/V0.5 real-data and persistence boundaries where they already exist instead of inventing new semantics.
- Runtime cleanup is allowed only for documented obsolete artifacts inside browser/runtime-derived data surfaces.

## Explicit no-go items

- No AI advisor implementation.
- No silent promotion of static shell routes to “accepted”.
- No metric formula changes.
- No deletion of evidence, raw business samples, secrets, browser sessions, or release materials.

## Initial execution strategy

1. Create a gap matrix from blueprint vs current V2 pages vs available V1/V0.5 reusable data paths.
2. Inventory candidate deletions before touching any runtime history artifact.
3. Implement the next blueprint-priority V2 route slice using the approved V2 visual language and existing verified data boundaries.
4. Validate locally, run required regressions, deploy, and record remaining owner-review gates.
