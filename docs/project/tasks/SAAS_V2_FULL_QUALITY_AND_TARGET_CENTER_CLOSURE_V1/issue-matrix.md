# Issue Matrix

Status: `IMPLEMENTATION_AUDIT_IN_PROGRESS`

| ID | Route / Layer | Issue / finding | Evidence | Treatment |
|---|---|---|---|---|
| P0-SSOT-ROUTE-TRUTH | `PROJECT_SSOT.json` | V2 route truth was stale: `routeCount=9`, `dataBoundRoutes=[/v2/home]`, `remainingStaticShellRouteCount=8`. | Current source and previous public route evidence show ten V2 routes. The ten routes are not all the same kind of data binding. | Updated SSOT to `routeCount=10`; split 7 reader routes, `/v2/upload` runtime writer, `/v2/search-assets` configuration writer, and `/v2/exclusion-rules` safe blocked route. |
| P0-ROUTE-MATRIX-STALE | `ROUTE_DATA_SOURCE_MATRIX.json` | V2 route entries still described static constants for routes already replaced by runtime adapters, V2 embedded upload/quality/history clients, target management, search config, or safe blocked states. | Matrix entries for `/v2/series-board`, `/v2/upload`, `/v2/target-center`, `/v2/search-assets`, and `/v2/exclusion-rules` conflicted with current code. | Updated V2 route matrix with `bindingClass`; added missing `/v2/upload/history`; kept `/v2/upload` and `/v2/search-assets` out of data-bound dashboard route count. |
| P0-TARGET-WRITE-COPY | `/v2/target-center` | Target drawer still said “保存前不会写入本地数据”, which is false once the user clicks save. | `components/targets/v05/target-management-client.tsx` drawer copy vs `saveTargetManagementChange` prepare/readback/activate path. | Changed copy to state that save writes current-browser target data and becomes effective after read-back validation. |
| P0-TARGET-PERCENT-INPUT | `/v2/target-center` | Percent targets used `<input type="number">`; values such as `92` could be saved as `9200%` instead of normalized `92%`. | Blueprint percent rule allows `92`, `92%`, `0.92`; current save used `Number(targetValueText)`. | Added parser for percent targets: `92`, `92%`, `0.92` all normalize to `0.92`; mutation rejects unnormalized percent values above `1`. |
| P1-TARGET-HIERARCHY-STATUS | `/v2/target-center` | Needs explicit validation that V2 target-center preserves V0.5F frozen hierarchy/status behavior. | `docs/releases/v0.5f-target-allocation-freeze.md` freezes new/edit/pause/reactivate and no hard delete. | Added target-center boundary panel; no delete/void action added; validator checks pause/reactivate remain and hard delete stays closed. |
| P1-TARGET_AGENT_REQUIRED_CONFLICT_STALE | target contracts | `docs/agents/target-agent.md` lists 品牌词访客 / 品牌词支付人数 as required target inputs, but current stable registry and validators classify them as unsupported target inputs. | `docs/project/V2_HOME_DATA_CONTRACT.json`, `lib/bi/target-metric-definitions.ts`, and validators use stable registry; latest owner/control instruction says keep registry and validator as authority. | Recorded as stale/conflict. Do not expand required inputs from target-agent; do not invent brand-word target schema. |
| P1-TARGET-MULTI-PERIOD-GAP | `/v2/target-center` | V0.5F target-center only has daily/monthly `periodType`; no approved week/custom/multi-month target schema. | Freeze says week/custom have no explicit target and must not auto-convert; blueprint says no single-month reuse for multi-month/custom without covering target. | UI now states day/single-month only; week/custom/multi-month stays `BLOCKED_BY_MISSING_CONTRACT` for target input, not inferred. |
| P1-TARGET-DATASET-LAYERING | `/v2/upload` → `/v2/target-center` | 18-file runtime dataset and V0.5F target foundation are intentionally separate. Forcing runtime snapshots into `airburg-v05` would corrupt target semantics. | `loadTargetManagementContext()` reads active V0.5F dataset; 18-file upload writes `airburg-runtime-dataset-v1`; user/control clarified this split is correct. | Added V2 upload four-source target foundation section reusing existing V0.5F import; target-center empty state explains when runtime data exists but target foundation is missing. No runtime-to-V0.5 write bridge added. |
| P0-PUBLIC-HTTP-SHA256-PROVIDER | `/v2/upload` target foundation / V0.5 migration hash paths | Public HTTP IP is not a secure context, so `crypto.subtle` is unavailable and SHA-256 calls can fail even though localhost passes. | Narrow CDP eval on `http://123.57.49.121/v2/upload` returned `isSecureContext=false`, `hasCrypto=true`, `hasSubtleCrypto=false`, `cryptoKeys=[getRandomValues]`; two public four-source runs failed at the import-completion layer while local passed. MDN documents `Crypto.subtle` as secure-context only and `Window.crypto` as limited to `getRandomValues` in insecure contexts. | Added shared SHA-256 provider: WebCrypto fast path when available, audited `@noble/hashes/sha2.js` fallback otherwise. Reused it from `lib/v05/import/hash.ts` and `lib/v05/migration/hash.ts`; added standard vector + forced-fallback equivalence validator. |
| P0-HOME-RUNTIME-V05-BOUNDARY | `/v2/home` / `lib/bi/bi.data-source.ts` | After public regression cleanup removed runtime/debug and legacy compatibility state while preserving `airburg-v05`, `/v2/home` still showed 17 metrics by falling through to the V0.5F target foundation dataset. This made it impossible to both preserve the target foundation and return the homepage to empty state. | Same-profile public storage diagnostic showed `localStorageKeys=["airburg:demo-session"]`, runtime/debug record counts 0, `airburg-v05` preserved with four-source facts, and `/v2/home` still rendered 17 metrics. | Added a source boundary option to `loadHomeBIDataSource`; `/v2/home` now calls it with `includeV05Persistence:false`, so home uses runtime/in-memory or runtime snapshot data but does not treat target-center foundation as homepage经营数据. Validators now lock this boundary and cleanup empty-state evidence. |
| P1-TARGET-DRAWER-DUPLICATE-LABEL | `/v2/target-center` | Store/series/product target drawer showed duplicate “平台和店铺” label risk. | Static source review and browser drawer inspection. | Source keeps a single label; closure validator counts one source label and upload18 E2E counts one visible drawer label after selecting store scope. |
| P1-SEARCH-COPY | `/v2/search-assets` | Search-assets page still exposed route paths in user-facing copy and could feel like an engineering note. | Source copy referenced `/v2/home`, `/v2/series-board`, `/v2/product-board`. | Reworded to 首页 / 系列看板 / 商品看板 while preserving current-browser configuration truth. |
| P1-EXCLUSION-COPY | `/v2/exclusion-rules` | Exclusion-rules page put `BLOCKED_BY_MISSING_CONTRACT` in business-facing body copy. | Source copy used inline code and route paths. | Kept status badge for evidence; changed body copy to business-safe explanation and no fake controls. |
| P1-TEN-ROUTE-QUALITY | ten `/v2/*` routes | Full browser issue inventory needs live route/mobile/console/network evidence locally and post-deploy. | Latest user requested complete issue inventory, not single-page patch. | Added ten-route system Chrome validator; local pass captured desktop/mobile screenshots for all ten V2 routes. Public pass remains pending until deployment. |
