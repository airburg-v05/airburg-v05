# Plan

1. Preflight and archive setup.
2. Rebuild ten-route truth from source, SSOT, route matrix, contracts, and browser evidence. `LOCAL_PASS`: source-backed SSOT/matrix truth corrected; local ten-route desktop/mobile browser evidence captured.
3. Deep target-center audit. `LOCAL_PASS`: V0.5F freeze, target registry conflict, hierarchy/status, percent input, write-copy, dataset layering, and duplicate store label issues audited.
4. Implement in-scope fixes across SSOT/docs/source/UI/persistence only where a stable contract exists. `LOCAL_PASS`: no schema/ETL/formula change; runtime and V0.5F target foundation remain separated.
5. Add validators/tests for every fix. `LOCAL_PASS`: route-truth/target-center closure validator, upload18+target E2E, and ten-route Chrome validator now cover the fixes.
6. Run changed-file lint, repo lint, build, targeted validators, and real system Chrome upload18 regression. `LOCAL_PASS`.
7. Commit implementation once.
8. Deploy through the existing Aliyun flow.
9. Public ten-route regression, PM2/Nginx/port checks, desktop/mobile/console/network checks.
10. Clear this-run public runtime/debug test records and verify `/v2/home` empty state + `/v2/upload` CTA.
11. Write validation/result/handoff; make a separate evidence commit if needed; leave worktree clean.

Optimization limit:

- At most three rounds.
- If the same root cause repeats twice without measurable improvement, switch to a replacement route and preserve failure evidence.
